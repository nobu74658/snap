import 'package:flutter/material.dart';

class WorkListPage extends StatelessWidget {
  const WorkListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("WorkListPage"),
    );
  }
}
