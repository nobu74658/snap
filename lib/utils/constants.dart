enum UploadType {
  GALLERY,
  CAMERA,
}

enum PostCaptionOpenMode {
  FROM_POST,
  FROM_FEED,
}

enum FeedMode {
  FROM_HEAD,
  FROM_LIST,
}

enum CommentLocationMode {
  FROM_POST,
  FROM_COMMENT,
}